kind: pipeline
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: integration
    image: node
    commands:
    - npm install
    - npm run build
    - npm run test
    - npm run lint 
