kind: pipeline
type: docker
name: windows

platform:
  os: windows
  arch: amd64

steps:
  - name: integration
    image: node:12
    commands:
    - npm ci
    - npm run build
    - npm run test
    - npm run lint 

