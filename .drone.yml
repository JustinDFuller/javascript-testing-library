kind: pipeline
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: integration
    image: node:12
    commands:
    - npm ci
    - npm run build
    - npm run test
    - npm run lint 
